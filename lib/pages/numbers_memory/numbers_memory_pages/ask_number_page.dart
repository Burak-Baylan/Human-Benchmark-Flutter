import 'package:flutter/material.dart';

class AskNumber extends StatelessWidget {
  const AskNumber({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}