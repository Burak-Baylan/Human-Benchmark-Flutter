import 'package:flutter/material.dart';

class ShowMsPage extends StatelessWidget {
  const ShowMsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}